
import React from 'react';
import { Navigate } from 'react-router-dom';

// This page has been replaced with separate page components
const Index = () => {
  return <Navigate to="/" replace />;
};

export default Index;
