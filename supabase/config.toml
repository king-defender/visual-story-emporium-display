# Replace with your real project ref once created (Settings -> General -> Reference ID),
# or run `npx supabase link --project-ref <ref>` which rewrites this file for you.
project_id = "REPLACE_WITH_YOUR_SUPABASE_PROJECT_REF"
